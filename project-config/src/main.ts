/**
 * This function takes a string and returns the first character.
 * All of the functions in this package should have comments
 * in this format.
 *
 * @param message - Any string
 * @returns The first character in the `message` string
 */
function myExportedFunction(message: string) {
  return message.charAt(0)
}

/**
 * Add config logic here
 */
